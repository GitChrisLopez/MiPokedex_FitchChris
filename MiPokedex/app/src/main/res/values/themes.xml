<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MiPokedex_FitchChris" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/mustard</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnPrimary">@color/mustard</item>
        <item name="android:windowBackground">@drawable/fondo</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MiPokedex_FitchChris" parent="Base.Theme.MiPokedex_FitchChris" />
</resources>